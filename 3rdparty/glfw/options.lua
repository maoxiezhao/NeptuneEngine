-- registers a new command-line option.

newoption 
{
	trigger = "build_shader_libs",
	description = "build shader libs."
}

newoption 
{
	trigger = "build_examples",
	description = "build examples."
}

newoption 
{
	trigger = "build_tests",
	description = "build tests."
}

newoption 
{
	trigger = "install",
	description = "Generate installation target."
}